PNAS adapter hotfix
===================

You do NOT need to revert the whole project.

Replace only:

    src/psyview/data/pnas_psychophysics.py

with the file in this ZIP.

This fixes the indentation/nesting error from the previous PNAS box-plot patch.

The intended structure is restored:

    class PNASAdapter:
        ...
        def get_plot(...):
            ...
        def interactive_threshold_box_plot(...):
            ...
        def archived_plot(...):
            ...

So all existing PSF/CSF plotting should return, while keeping:

    Subject          -> PSF vs luminance
    Luminance        -> CSF
    Spatial frequency-> threshold box plot
    Staircase        -> individual trajectory/reversals

No empirical data files are changed.

Run:

    .\run_psyview.bat
